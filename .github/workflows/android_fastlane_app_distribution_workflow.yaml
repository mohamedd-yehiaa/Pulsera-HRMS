name: Android Fastlane with Firebase App Distribution Workflow

on:
  push:
    branches:
      - master

# Silences the GitHub Actions Node 20 deprecation warning
env:
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: "true"

jobs:
  distribute_to_firebase:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout my repo code
        uses: actions/checkout@v4

      - name: Set up JDK 17
        uses: actions/setup-java@v4
        with:
          java-version: '17'
          distribution: 'temurin'

      - name: Install Flutter
        uses: subosito/flutter-action@v2
        with:
          channel: stable

      - name: Setup Ruby
        uses: ruby/setup-ruby@v1
        with:
          ruby-version: "4.0.5"
          bundler-cache: true
          working-directory: ./android

      # 1. INJECT GOOGLE SERVICES JSON
      - name: Inject google-services.json
        env:
          GOOGLE_SERVICES_JSON: ${{ secrets.GOOGLE_SERVICES_JSON }}
        run: echo $GOOGLE_SERVICES_JSON | base64 -d > android/app/google-services.json

      # 2. INJECT FIREBASE OPTIONS DART
      - name: Inject firebase_options.dart
        env:
          FIREBASE_OPTIONS_BASE64: ${{ secrets.FIREBASE_OPTIONS_BASE64 }}
        run: echo $FIREBASE_OPTIONS_BASE64 | base64 -d > lib/firebase_options.dart

      # Ensures Flutter dependencies are fetched before building
      - name: Install Flutter Dependencies
        run: flutter pub get

      # Fixes Linux permission error before running Gradle builds
      - name: Make gradlew executable
        run: chmod +x ./android/gradlew

      - name: Distribute App with Fastlane
        env:
          FIREBASE_CLI_TOKEN: ${{ secrets.FIREBASE_CLI_TOKEN }}
          FIREBASE_APP_ID: ${{ secrets.FIREBASE_APP_ID }}
        run: |
          bundle install
          bundle exec fastlane android firebase_distribution
        working-directory: ./android