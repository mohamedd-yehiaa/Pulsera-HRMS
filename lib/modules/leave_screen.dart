import 'package:flutter/material.dart';

class LeaveScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return
        SafeArea(
            child:
            Text("Leave Screen",style: TextStyle(fontSize: 30,fontWeight: FontWeight.bold),),
        );

  }
}
